export function AuthEntryScreen({ onSignIn, onContinue }: { onSignIn: () => void; onContinue: () => void }) {
  return <div className="flex min-h-[100dvh] items-center justify-center bg-zinc-950 px-6 text-white"><div className="w-full max-w-sm rounded-3xl border border-white/10 bg-zinc-900/80 p-6 shadow-2xl"><p className="text-xs font-black uppercase tracking-[.24em] text-emerald-400">Football Tracker</p><h1 className="mt-3 text-3xl font-bold tracking-tight">Your season, safely tracked.</h1><p className="mt-3 text-sm leading-6 text-zinc-400">Sign in to back up matches across devices, or keep using this device locally.</p><button type="button" onClick={onSignIn} className="mt-7 w-full rounded-xl bg-white px-4 py-3 text-sm font-bold text-black">Sign in with Google</button><button type="button" onClick={onContinue} className="mt-3 w-full rounded-xl bg-zinc-800 px-4 py-3 text-sm font-semibold text-zinc-200">Continue without signing in</button></div></div>
}
