# v2.2.8 Awards, News, Records, and View All Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver v2.2.8 with canonical scoped award results, consistent Best XI presentation, newest-first award News, reusable long-list scroll-to-top UX, and corrected Player Records ordering.

**Architecture:** Award scopes produce a shared result object containing the match anchor, scoped player stats, best player, and Best XI. UI and News consume this result. A reusable observer-based control owns fixed scroll-to-top behavior independently of list rendering.

**Tech Stack:** React 19, TypeScript, Vite, Node test runner, oxlint.

**Spec:** `docs/superpowers/specs/2026-09-18-v228-awards-news-design.md`

## Global Constraints

- Keep all rating formulae and competition rules unchanged.
- Keep `RATING_ENGINE_REVISION = 8` and storage key `football-tracker-v1`.
- Use existing block notation exactly: `Season N-B`.
- Do not commit, push, reset, stash, checkout, or discard work.

---

### Task 1: Canonical scoped award results

**Files:**
- Modify: `src/engine/seasonAnalytics.ts`, `src/engine/awards.ts`, `src/engine/stats.ts`
- Test: `tests/v228-awards-news.test.cjs`

**Interfaces:**
- Produces a canonical result with `label`, `scopeLabel`, `statsByPlayer`, `bestPlayerId`, `bestXI`, and `anchorMatch`.
- Consumers: competition, history, Best XI presentation, and News.

- [ ] Write failing real-data tests for league block, Cup, and Champions scoped results.
- [ ] Run the focused test and observe missing canonical result behavior.
- [ ] Implement the smallest shared result derivation that reuses existing selectors/ranking data.
- [ ] Re-run the focused test and verify deterministic scopes, labels, and stats.

### Task 2: Shared award Best XI presentation

**Files:**
- Create: `src/components/AwardBestXI.tsx`
- Modify: `src/components/Pitch.tsx`, `src/screens/CompetitionScreen.tsx`, `src/screens/SeasonHighlightScreen.tsx`, `src/screens/SeasonRecapScreen.tsx`
- Test: `tests/v228-awards-news.test.cjs`

**Interfaces:**
- Consumes canonical award results from Task 1.
- Renders the existing Pitch with scoped `statsByPlayer` and `motmPlayerId`.

- [ ] Write failing behavior tests for labels and blue/star best-player presentation.
- [ ] Run the focused test and observe the missing shared presentation contract.
- [ ] Add the component and migrate award-style Best XI surfaces without changing selection.
- [ ] Re-run the focused test and verify all award types use the shared presentation.

### Task 3: Records grouping and shared scroll control

**Files:**
- Create: `src/components/FloatingScrollToTop.tsx`
- Modify: `src/screens/RecordsScreen.tsx`, `src/screens/GlobalRankingScreen.tsx`, `src/screens/SeasonHighlightScreen.tsx`, `src/index.css`
- Test: `tests/v228-scroll-records.test.cjs`

**Interfaces:**
- `FloatingScrollToTop` observes a selector sentinel and calls smooth document scroll.
- Player Records feeds `Most 10.0 Ratings` into `Leaderboard` directly after 9.0+.

- [ ] Write failing tests for exact-10 grouping/order and observer/button behavior.
- [ ] Run the focused test and observe missing shared behavior.
- [ ] Implement the reusable control and mount it on appropriate full/expanded list surfaces.
- [ ] Re-run the focused test and verify visibility, fixed positioning, smooth scrolling, and reuse.

### Task 4: Canonical award News and release metadata

**Files:**
- Modify: `src/engine/news.ts`, `src/config.ts`, `package.json`, `package-lock.json`, release tests
- Test: `tests/v228-awards-news.test.cjs`, `tests/v228-scroll-records.test.cjs`

**Interfaces:**
- News consumes Task 1 results and sorts `date DESC, id DESC`.

- [ ] Write failing tests for newest-first equal-date order, separate award entries, block labels, and stable IDs.
- [ ] Run focused tests and observe the existing combined/month-labelled behavior fail.
- [ ] Implement News projection and bump genuine version metadata to 2.2.8.
- [ ] Run focused tests and confirm the canonical feed result.

### Task 5: Full verification

**Files:**
- Inspect only: `dist/`, current diffs, status.

- [ ] Run `npm.cmd test`.
- [ ] Run `npm.cmd run lint`.
- [ ] Run `npm.cmd run build` and inspect the generated files for release strings.
- [ ] Run `git diff --check`, `git status --short`, `git diff --stat`, and inspect final meaningful diffs.
